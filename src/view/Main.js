import React, {Component} from 'react';
import RouterIndex from '../router'
class Main extends Component {
    render() {
        return (
            <div>
                <RouterIndex />
            </div>

        );
    }
}

export default Main;